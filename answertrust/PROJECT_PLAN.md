# AnswerTrust MVP boundary

The resume milestone is complete when the paper-grounded claim pipeline, evidence display, academic failure checks, review audit trail, retries, and 50-example benchmark remain green under `python -m pytest -q` and `python -m src.experiments`.

Further retrieval, multi-paper synthesis, model integration, cloud deployment, and frontend work are intentionally out of scope.
