# Production profile keeps operational request summaries while suppressing framework and SQL debug noise.
logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: WARN
    org.hibernate.orm.jdbc.bind: WARN

# TLS terminates at the deployment edge. Honor its Forwarded/X-Forwarded-* headers so
# Spring identifies the original scheme and emits HTTPS-only policies such as HSTS.
server:
  forward-headers-strategy: framework

# Production logging and runtime settings for the Spring API.
spring:
  datasource:
    url: jdbc:${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    show-sql: false

toodle:
  jwt:
    secret: ${JWT_SECRET}
